define(['react'], function (React) {
    'use strict';
    return React.createClass({
        displayName: 'MainApp',
        render: function () {
            return (
                <div>MainApp</div>
            );
        }
    });
});
